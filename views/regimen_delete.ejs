<h1><%= title %>: <strong> <%= regimen.summary %> </strong></h1>

<% if (regimen_prescriptions.length) { %>

<p>
  <strong
    >Delete the following prescriptions before attempting to delete this
    regimen.</strong
  >
</p>

<div style="margin-left: 20px; margin-top: 20px">
  <h4>Prescriptions</h4>
  <dl>
    <% for (const prescription of regimen_prescriptions) { %>
    <dt>
      <%= prescription.patient.name%> - <%= prescription.medication.name%>, <%=
      prescription.regimen.summary%> -
      <a href="<%= prescription.url %>">Detail</a>
    </dt>
    <% } %>
  </dl>
</div>

<% } else { %>

<p>Do you really want to delete this regimen?</p>

<form method="POST">
  <div class="form-group">
    <input
      class="form-control"
      type="hidden"
      id="regimenid"
      name="regimenid"
      value="<%= regimen._id %>"
    />

    <button class="btn btn-primary" type="submit">Delete</button>
  </div>
</form>

<% } %>
